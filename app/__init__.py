"""Laminario server: the HTTP API and, from U4, the processing worker.

This directory is the application source, imported from the repository root (``uvicorn app.main:app``).
It is not an installable package.
"""
