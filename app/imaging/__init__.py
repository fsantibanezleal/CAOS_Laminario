"""The imaging engine: readers, pyramids, the z-plane policy, extended depth of field, derivatives.

Used by the processing worker for uploads and by the base-collection scripts. Everything here is a pure
function of its inputs and writes only where it is told to.
"""
